fn calc(n: i32, b: bool) -> i32 {
    let mut a: [i32; 7];
    let mut f: bool;
    let mut x: i32;
    let mut fs: [bool; 24];

    return 42;
}

fn main() {
    let mut m: i32;
    let mut flag: bool;

    println(m, flag);
}
