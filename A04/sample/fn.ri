fn f(x: i32, a: bool) -> bool {
    let mut a: i32;
    a = 5;
}

fn main() {
    let mut b: bool;
    let mut x: i32;
    b = f(1, false);
    x = f(1, false);
    b = f(1, 1);
    b = f(1, 2, 3);
    b = f(1, false, 17);
}